<h1>Home</h1>

<form action="/index" method="post">
        
<% selectedFiles.forEach(function(selectedFile){ %>
    <input type="checkbox" name="<%= selectedFile %>">
  <label> <%= selectedFile %></label><br>
    <% }); %>

    <button type="submit">Confirm to index</button>
     
</form>